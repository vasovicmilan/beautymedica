import { Router } from 'express';

// import { validateCSRF } from "../../middlewares/validators/csrf.validator.js";
// import { validateSpam } from '../../middlewares/validators/spam.validator.js';

const router = Router();

// GET rute za renderovanje stranica
// router.get("/moj-profil", userController.getMyProfilePage);
// router.get("/termin-detalji/:appointmentId", );

// POST rute za obradu formi (direktno render)
// router.post("/promena-termina", validateCSRF, validateSpam, );
// router.post("/otkazivanje-termina", validateCSRF, validateSpam, );

export default router;
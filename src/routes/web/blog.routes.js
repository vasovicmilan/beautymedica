// src/routes/web/blog.routes.js

import { Router } from "express";
// import * as blogController from "../../controllers/web/blog/blog.controller.js";

// import { validateCSRF } from "../../middlewares/validators/csrf.validator.js";
// import { validateSpam } from '../../middlewares/validators/spam.validator.js';
// import { validateSearch } from "../../middlewares/validators/search.validator.js";

const router = Router();

// router.get("/", blogController.getBlogPage);
// router.get("/pretraga", blogController.getSearchPage);
// router.get("/objava/:postSlug", blogController.getPostDetailsPage);
// router.get("/kategorija/:categorySlug", blogController.getCategoryPage);
// router.get("/oznaka/:tagSlug", blogController.getTagPage);
// router.post("/pretraga", validateCSRF, validateSpam, validateSearch, blogController.postSearchRedirect);

export default router;